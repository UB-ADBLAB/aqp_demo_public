export const webSocketAddress = `ws://${window.location.hostname}:${window.location.port}`;
export const backendUrl = `${window.location.href}`;
export const clientConfigUrl = `${backendUrl}getClientConfig`;
export const adhocQueryUrl = `${backendUrl}adhocQuery`;
export const txnWorkloadRunUrl = `${backendUrl}txnWorkloadRun`;
export const txnWorkloadStopUrl = `${backendUrl}txnWorkloadStop`;

